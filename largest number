//program to find the largest amomg two numbers.
#include<stdio.h>
int main(){
    int x,y;
    printf("enter the value of x and y :", x,y);
    scanf("%d" "%d",&x, &y);
    if (x>y)
       printf("%d  is largest ",x);
    else 
       printf("%d is largest",y);
    return 0 ;



}
