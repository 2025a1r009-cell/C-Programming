//Program to check given character is alphabet , digit or special symbol.
#include<stdio.h>
int main(){
    char ch ;
    printf("enter character : ");
    scanf("%c", &ch );
    if ((ch>=65 && ch<=97 && ch<=122))
         printf("%c is alphabet/n",ch);
    else if(ch>=48 && ch<=57)
         printf("%c is digit\n",ch);
    else
        printf("%ch is special character",ch);
    return 0;
}
