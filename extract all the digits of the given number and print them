// WAP to extract all the digits of the given number and print them
#include<stdio.h>
int main(){
    int num;
    printf("enter the number  :");
    scanf("%d",&num);
    while (num!=0){
        int rem = num%10;
        printf("%d",rem);
        num=num/10;
    }
   
    return 0 ;
}
